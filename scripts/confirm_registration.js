function confirm_settings(node) {
  return confirm("Registration confirmed! Check your email for a copy of a receipt and event details!");
}

/*   if (x.className === "topnav") {
    x.className += " responsive";
  } else {
    x.className = "topnav";
  }
*/